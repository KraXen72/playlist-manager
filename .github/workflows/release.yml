name: Release

on:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: latest
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm run dist

      - name: Copy AppImages as raw binaries for mise (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          for appimage in dist/playlist-manager-*-linux-x64.AppImage; do
            [ -f "$appimage" ] && cp "$appimage" dist/playlist-manager-linux-x64
          done
          for appimage in dist/playlist-manager-*-linux-arm64.AppImage; do
            [ -f "$appimage" ] && cp "$appimage" dist/playlist-manager-linux-arm64
          done

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.os }}
          path: |
            dist/playlist-manager-*.exe
            dist/playlist-manager-*.dmg
            dist/playlist-manager-linux-x64
            dist/playlist-manager-linux-arm64

  release:
    needs: build

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: binaries-*/*
          generate_release_notes: true
